@model IEnumerable<MovieReview>

<!DOCTYPE html>

<html>
<head>
    <title>title</title>
</head>
<body>

    <div>

        @Html.AntiForgeryToken()
        @* <table class="table table-striped table-bordered table-hover"> *@
        @*     <thead> *@
        @*     <tr> *@
        @*         <th>MovieName</th> *@
        @*         <th>Rating</th> *@
        @*         <th>Description</th> *@
        @*         <th style="width: 0"></th> *@
        @*     </tr> *@
        @*     </thead> *@
        @*     <tbody> *@
        @*     @foreach (var item in Model) *@
        @*     { *@
        @*         <tr id="@item.id"> *@
        @*             <td>@item.Name</td> *@
        @*             <td>@item.Rating</td> *@
        @*             <td>@item.Description</td> *@
        @*             <td><button class="btn btn-danger" onclick="ListRemove(@item.id)"><i class="glyphicon glyphicon-remove"></i></button></td> *@
        @*         </tr> *@
        @*     } *@
        @*     </tbody> *@
        @*      *@
        @* </table> *@
        
        
        <div class="dataBaseGrid">
            @foreach (var item in Model)
            {
                <div class="card">
                    <div class="card-body">
                        @* <img src="@Url.Content("https://via.placeholder.com/250/")" class="card-img-top"> *@
                        <h4 class="card-title">@item.Name</h4>
                        <h6 class="card-subtitle mb-2 text-muted">@item.Rating</h6>
                        <p class="card-text">@item.Description</p>
                        <button class="btn btn-danger card-link" onclick="ListRemove(@item.id)"><i class="glyphicon glyphicon-remove"></i></button>
                        @* <a href="#" class="card-link">Card link</a> *@
                        @* <a href="#" class="card-link">Another link</a> *@
                    </div>
                </div>
            }
        </div>
    </div>

    <script>
    function ListRemove(thingId) {
        $.ajax({
            url: "/DataBase/ActionRemoveItem",
            type: "POST",
            data: { itemId: thingId },
            success: function (data) {
                // $("#" + id).remove();
                window.location.reload();
            }
        });
    }
</script>

</body>
</html>